# Kopier denne fila til ".env" og fyll inn verdiene fra Supabase
# (prosjektet ditt → Settings → API).
# .env skal IKKE i GitHub – den ligger i .gitignore.

VITE_SUPABASE_URL=https://dittprosjekt.supabase.co
VITE_SUPABASE_ANON_KEY=din-anon-public-key
